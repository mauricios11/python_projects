# conda env create --file enviroment.yml
# mamba env create --file

name: python_projects
channels: 
  - anaconda
  - conda-forge
  - defaults
dependencies:
  - black
  - invoke
  - fs
  - jupyter
  - jupyterlab
  - matplotlib
  - numpy
  - pandas
  - pathlib
  - pip
  - pyprojroot
  - python=3.9
  - seaborn
  - scipy
  - pip:
    - pyhere
    